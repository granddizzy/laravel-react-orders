# Используем официальный образ Node.js
FROM node:18

VOLUME /code

# Устанавливаем рабочую директорию
WORKDIR /code

# Открываем порт, на котором будет работать приложение (по умолчанию React работает на 3000)
EXPOSE 3000